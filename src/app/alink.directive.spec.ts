import { AlinkDirective } from './alink.directive';

describe('AlinkDirective', () => {
  it('should create an instance', () => {
    const directive = new AlinkDirective();
    expect(directive).toBeTruthy();
  });
});
